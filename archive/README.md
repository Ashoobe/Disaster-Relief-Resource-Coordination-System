This folder holds non-active project artifacts that are kept only for reference.

- `application-prototype/` is an archived alternate frontend scaffold.
- The live frontend is in `frontend/`.
- Nothing in `archive/` is part of the current Vite build or backend runtime.
